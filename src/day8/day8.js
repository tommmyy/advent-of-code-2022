const parse = input =>
  input
    .split('\n')
    .filter(Boolean)

const compute = input => {
  const parsed = parse(input);
  // TODO
  return parsed;
};

export const part1 = input => {
  const result = compute(input);
  // TODO
  return result;
};


export const part2 = input => {
  const result = compute(input);
  // TODO
  return result;
};
